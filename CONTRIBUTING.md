# Contributing

## How to contribute

1. Fork the repo and create a branch
2. Make your changes
3. Open a pull request — CI runs `cargo build --release` and `cargo test`
4. Wait for review and merge

Direct pushes to `master` are restricted to trusted maintainers.

## Local development

```
cargo build
cargo test
cargo build --release
```
